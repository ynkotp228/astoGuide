---
const { frontmatter } = Astro.props;
---
<meta charset="utf-8" />
<h1>{frontmatter.title}</h1>
<p>{frontmatter.pubDate.slice(0, 10)}</p>
<p>{frontmatter.description}</p>
<slot />
<img src={frontmatter.image.url} alt={frontmatter.image.alt} width="300" height="300px"/>